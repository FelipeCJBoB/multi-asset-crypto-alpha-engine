# audit/n_lifetime.yaml
#
# Ledger de trials vitalício — §16.10.2 M5. O DSR (§11.6) usa `counter`, não o N
# de uma busca isolada. Incrementa em: cada constante classe B otimizada, cada
# retreino, cada challenger, cada emenda ao pré-registro (§16.9), cada
# experimento §9.5.1 (RPI vs post-only). NÃO decrementa nunca — trial gasto é
# gasto mesmo que a variante perca.
#
# Regra de edição: só se acrescenta ao final de `log`. Nunca editar ou remover
# uma entrada já registrada — isso é o próprio objeto que o ledger existe para
# impedir.
#
# Regra de contagem — 1 trial em bloco vs. N trials (AG-039, fechado
# 2026-08-16). Não havia fórmula declarada até aqui, só "decisão do Manager
# caso a caso" (PLANO_MESTRE_PRINCE2.md:1164-1165/1421-1423) — o log já
# demonstrava a distinção na prática (id 10 vs. id 11 abaixo), só nunca
# generalizada. Regra explícita, extraída do próprio precedente:
#
#   N trials = nº de combinações de parâmetro que exigem um AJUSTE DE MODELO
#   OU RECÁLCULO DE BACKTEST novo por combinação (cada ponto é uma decisão
#   testada contra dado real). Exemplo: id 10 — grade tp_atr_mult x
#   sl_atr_mult, 3x3 por lado = 18 combinações, cada uma resolvendo a
#   barreira de novo -> delta=18.
#
#   1 trial = passe de RANKING/TRIAGEM que reusa um artefato já ajustado
#   (nenhum modelo novo treinado, nenhum backtest novo por candidata) —
#   conta como 1 INDEPENDENTE do nº de candidatas comparadas. Exemplo:
#   id 11 — 70 candidatas rankeadas por ortogonalidade incremental sobre
#   correlação já computada, "nenhum modelo retreinado" -> delta=1.
#
#   Sweep de sensibilidade ±50% de constante classe A (§16.10 regra 4,
#   obrigatório antes do Gate 3) segue a mesma régua: se cada ponto do sweep
#   (ex. -50%/baseline/+50%) dispara um retreino/backtest novo, delta = nº de
#   pontos testados; se o sweep é lido de um backtest/modelo já existente sem
#   reajuste, delta=1. Aplica-se aos sweeps já agendados em §11.4 (Sprint 5/6/
#   10/11/16) no momento em que cada um de fato rodar.

# ============================================================================
# DESCONTINUADO como orçamento vinculante -- 2026-08-17, decisão do Manager
# ("pode descontinuar, não nos será útil pra esse projeto, foi mal
# implementado desde o começo"). O ledger continua existindo como registro
# HISTÓRICO (append-only preservado, nada abaixo foi apagado/editado -- mesma
# disciplina de sempre) mas `counter` deixa de bloquear trabalho em
# `PLANO_MESTRE_PRINCE2.md` §11.4/§11.6 (M4/V41-6/V41-7/V41-10 não ficam mais
# presas por `counter > 60`).
#
# Consequência real, NÃO resolvida por esta descontinuação sozinha:
# `PRD_V4_1.md` §6.1 usa `N_lifetime` como insumo direto da fórmula de DSR
# (piso de significância cresce com N) e §6.5 critério de encerramento #5
# ("N_lifetime > 60 sem Camada 2 fechada -> encerrar") depende deste contador
# -- `src/validation/dsr.py`/`src/analysis/faixa2_dsr_and_b2_check.py` leem
# `counter` de verdade (`_audited_n_lifetime()`). Descontinuar o CONTADOR sem
# decidir o que substitui a penalidade de multiple-testing no cálculo de DSR
# deixa o Gate 6 (V41-12) e o critério #5 sem definição operacional --
# registrado como `audit/architecture_gaps_log.yaml::AG-077`, decisão
# pendente do Manager (não inventada aqui, é decisão estatística/de
# governança que não é do Claude tomar sozinho).
# ============================================================================

schema_version: 1
status: "descontinuado_2026_08_17 -- nao bloqueia mais trabalho, ver AG-077 pro que substitui DSR/Gate 6"
counter: 223

log:
  - id: 1
    date: "2026-08-08"
    type: timeframe_search
    description: >
      Mudança de timeframe de decisão 5m → 1h, na elaboração do PRD V2/V3
      (histórico da conversa que produziu o blueprint, anterior a este repo).
    delta: 1
  - id: 2
    date: "2026-08-08"
    type: timeframe_search
    description: "Mudança de timeframe de decisão 1h → 30m."
    delta: 1
  - id: 3
    date: "2026-08-08"
    type: timeframe_search
    description: >
      Mudança de timeframe de decisão 30m → 15m (escolha a priori do PRD V3.2,
      §0.4, com base em ATR medido sobre amostra de 8 meses — ela própria P2,
      a refazer no Sprint 3).
    delta: 1
  - id: 4
    date: "2026-08-09"
    type: new_confidence_definition
    description: >
      Faixa 1.5, Bloco 4 — `confidence_rank` (percentil do score cru
      intra-fold) adicionado a `predictions.parquet` como definição nova de
      confiança, ao lado de `confidence` (calibrado) já existente. Não
      recalibra sobre OOF empilhado, não ajusta calibrador nenhum — mas é
      uma variante de definição de sinal (§5.12), variante de modelo pelo
      critério de M5.
    delta: 1
  - id: 5
    date: "2026-08-09"
    type: monotonic_constraint_variant
    description: >
      Faixa 1.6, Bloco 4 — retreino dos 15 folds x 2 lados da Camada 1 com a
      restrição forçada de `E02f_funding_z_expanding` REMOVIDA só do short
      (`unforce_features_by_side`, parâmetro aditivo novo em
      `src.models.monotonic`/`src.models.alpha`, default `None` em produção
      — `alpha_c1_v1`/`alpha_c0_baseline_v1` intocados). Model_id novo
      `alpha_c1_e02f_short_unforced_v1`, artefatos persistidos
      (predictions.parquet + diagnostics/) sem substituir nenhum existente.
      `directional_sharpe` pooled caiu de 0,879 (baseline atual, os dois
      lados forçados) para 0,282 — a maior parte do ganho do T0 vinha do
      short, não do long, apesar de o Bloco 5 da Faixa 1.6 ter medido que a
      triagem estatística quase nunca confirmaria essa restrição sozinha
      (1/15 folds com unanimidade). Variante de modelo pelo critério de M5.
    delta: 1

  - id: 6
    date: "2026-08-09"
    type: exploratory_comparison_b20
    description: >
      Faixa 2, F0.1 — auditoria retroativa. Faixa 1, D2 (`by_regime`,
      `src.analysis.calibration_diagnostics`): decis de confiança×retorno
      recomputados separadamente para 8 blocos lado×regime (2 lados x
      R1-R4). 1 dos 8 (long/R1) saiu 10/10 células `insuficiente` (n<200
      em toda célula) — nenhuma comparação estimável ali, então não é um
      "olhar" que pudesse ter produzido um achado (sem `mean_ret_net_bps`/
      CI/t-stat, só frequência). Os outros 7 blocos produziram Spearman
      estimável. delta=7, um por bloco lado×regime com estatística válida.
      Artefato: experiments/faixa1_calibration_diagnostic.json::D2.
    delta: 7
  - id: 7
    date: "2026-08-09"
    type: exploratory_comparison_b20
    description: >
      Faixa 2, F0.1 — auditoria retroativa. Faixa 1.5, Bloco 2
      (`decompose()` reusado, `stratified_headlines.by_side_regime`):
      `directional_sharpe`/`total_sharpe` computados para as 8 células
      lado×regime (2 lados x R1-R4), sem retreino. Reutilizado (não
      recomputado) na "Correção de método" da Faixa 1.6 para a leitura
      "short é positivo nos 4 regimes, long oscila" — o trial é datado da
      computação original (Faixa 1.5), não do reuso. delta=8, um por
      célula lado×regime. Artefato: experiments/faixa1_5_prerequisites.json
      ::stratified_headlines.by_side_regime.
    delta: 8
  - id: 8
    date: "2026-08-09"
    type: exploratory_comparison_b20
    description: >
      Faixa 2, F0.1 — auditoria retroativa. Faixa 1 (seção "Subconjunto
      congruente/incongruente"): partição binária de regimes {R1,R2,R3,R4}
      em congruente/incongruente com a restrição forçada de
      `E02f_funding_z_expanding`, decidida a partir do sinal do IC medido
      por regime (`ic_by_regime`, pooled OOF) — congruente do long =
      {R3,R4}, congruente do short = {R1,R2}. Duas escolhas distintas
      (uma por lado, sinais econômicos opostos), cada uma um "olhar"
      seletivo separado sobre o mesmo IC por regime. delta=2.
      Artefato: experiments/faixa1_calibration_diagnostic.json (seção de
      subconjunto congruente/incongruente).
    delta: 2
  - id: 9
    date: "2026-08-09"
    type: exploratory_comparison_b20
    description: >
      Faixa 2, F0.1 — auditoria retroativa. Faixa 1.7, Q1 medição 1: split
      do book long em R3 por direção de tendência causal de 48 barras
      (R3-alta n=6.067 vs R3-baixa n=378), usado como parte do critério
      pré-registrado Q1 (edge vs. beta). Uma comparação binária. delta=1.
      Artefato: experiments/faixa1_7_edge_or_beta.json
      ::q1_r3_split_por_direcao_de_tendencia.
    delta: 1
  - id: 10
    date: "2026-08-09"
    type: barrier_geometry_sweep
    description: >
      Faixa 2, FASE 2 E1 — varredura declarada ANTES da busca (B20) de
      tp_atr_mult x sl_atr_mult, grade 3x3 independente por lado (long:
      {1.5,2.0,2.5}x{1.0,1.5,2.0}; short: mesma grade) = 18 variantes.
      Resolução de barreira vetorizada via sliding_window_view
      (`src/labels/barrier_sweep.py`, §18.7.1, nunca implementado antes
      desta rodada) — reusa o FILL já persistido em
      labels/v1/labels.parquet (independente de tp/sl, ver docstring do
      módulo), recalcula só TP/SL/TIME por célula. Sanidade: célula
      central (tp=2.0, sl=1.5, config de produção atual) reproduz a
      distribuição pooled do Sprint 6 com max_abs_diff=2,3e-5. delta=18.
      Artefato: experiments/faixa2_e1_barrier_sweep.json.
    delta: 18
  - id: 11
    date: "2026-08-09"
    type: feature_orthogonality_research_pass
    description: >
      Faixa 2, FASE 2 E2 — passe de pesquisa LIBERADO pelo Manager sem
      curadoria: 70 candidatas T2 computáveis do catálogo §2.2-2.12
      (grupos A/B/C/D/E/H/K; F excluído por quebra de RPI §2.7.1, G/I sem
      dado, J excluído por desenho §5.8) avaliadas em bloco único contra
      o T1 de 8 (já reduzido pelo pré-E2 (2), id 10), rankeadas por
      ortogonalidade incremental (seleção gulosa maximizando
      hhi_efetivo/n_eff_factors a cada passo) até a faixa-alvo 12-15
      pedida pelo Manager. Nenhum modelo retreinado, nenhuma promoção a
      T1_FEATURE_IDS/registry.yaml em produção — é medição para a
      triagem in-fold do §5.4 (E3, ainda não implementado) decidir.
      Contado como 1 trial por instrução explícita do Manager ("conta
      como 1 trial, não um por feature") — não 70. delta=1.
      Artefato: experiments/faixa2_e2_research.json.
    delta: 1
  - id: 12
    date: "2026-08-09"
    type: data_quality_correction
    description: >
      Faixa 2, E2 — correção de dado (não busca nova, `delta=0`, mesmo
      tratamento do "bit-idêntico, n_lifetime += 0" de pré-E2 (2)/id ~10).
      Manager pediu verificar se a "defasagem de 75 dias" do on-chain
      (H01/H02/H03/H06/H08) era latência estrutural da fonte ou CSV local
      desatualizado. Verificado contra community-api.coinmetrics.io: dado
      publicado até 2026-08-08 (~1 dia de atraso real, não 75). O
      `data/capacity/onchain/btc_coinmetrics.csv` local estava de fato
      obsoleto (última linha 2026-05-24, arquivo de 2026-08-04) —
      atualizado via download direto da community API (2443 linhas,
      2019-12-01..2026-08-08; original preservado como
      `btc_coinmetrics.csv.stale_2026-05-24.bak`, arquivo gitignored, não
      versionado). Passo de pesquisa E2 (id 11) RE-RODADO com dado
      corrigido para confirmar robustez, não para re-buscar: seleção
      final IDÊNTICA (mesmas 10 candidatas, mesma ordem),
      `n_eff_factors` 15,825459850990606 -> 15,825275447201294 (Δ
      -0,0002, ruído). Conclusão do id 11 não muda; delta=0 porque não é
      um trial novo, é confirmação de que o já contado sobrevive à
      correção do bug.
    delta: 0
  - id: 13
    date: "2026-08-09"
    type: stability_screen_research_pass
    description: >
      Faixa 2, FASE 2 E3 — Camada 2 (§5.4) implementada
      (`src.models.stability.stability_screen`, `estabilidade = forca *
      consistencia**2`, denominador fixo 6) e rodada como passe de
      pesquisa único sobre as 18 candidatas (T1 reduzido de 8 + 10 do E2)
      x 15 splits REAIS do CPCV x 2 lados = 30 células por feature.
      `limiar` ASSUMED (constants.yaml::alpha_stability_screen_limiar=
      0,02, classe A, sweep_range [0,01;0,03] declarado). Nenhum modelo
      retreinado, nenhuma promoção a T1_FEATURE_IDS/registry.yaml.
      Ambientes = 6 (RANGE/TREND x tercil de custo), NÃO 12 — decisão do
      Manager corrigindo ambiguidade de vocabulário do PRD (ver
      PRD_V3_2_UNIFICADO.md §5.4, correção 2026-08-09); rpi_regime
      (PRE/POST) tratado como diagnóstico (`ic_by_rpi_regime`), não como
      ambiente cruzado, mesma decisão. Contado como 1 trial (mesmo
      raciocínio do E2 -- a triagem inteira é uma pergunta de desenho, não
      uma por feature). delta=1.
      Artefato: experiments/faixa2_e3_stability.json.
    delta: 1
  - id: 14
    date: "2026-08-09"
    type: selection_mechanism_variant
    description: >
      Faixa 2, pré-FASE 3 — teste do C07 (`C07_vol_pctile_expanding`) como
      ACELERADOR, substituindo o gate de confiança (§5.6), sobre
      predições JÁ EXISTENTES de `alpha_c1_v1` (SEM retreino). `side_hat`
      recomputado como `argmax(p_long, p_short)` (threshold de confiança
      no mínimo que ainda produz sinal); único filtro de admissão =
      percentil de C07, fixado A PRIORI pelo orçamento
      (`trades_per_year == 662,71`, bisseção sobre volume, nunca sobre
      performance — evita B20). Braço de controle: corte aleatório
      retendo a MESMA contagem por path, 1000 sementes
      (`alpha_b1_n_seeds`/`alpha_random_seed`, mesma convenção de
      `run_b1_random_entry`). Resultado: NÃO confirma a hipótese —
      `directional_sharpe` do filtro por vol (-1,01) fica no percentil 21
      do nulo aleatório (pior que ~79% dos sorteios aleatórios do mesmo
      tamanho); produção atual (+0,88) continua muito melhor que as duas
      alternativas. `ret_net` melhora modestamente sobre o aleatório
      (percentil 84) mas continua bem pior que a produção. Contado como
      variante de MECANISMO DE SELEÇÃO (side_hat recomputado + filtro
      novo aplicado), não medição pura — por instrução explícita do
      Manager ("é uma variante de mecanismo de seleção, não medição").
      delta=1. Nenhuma promoção a produção.
      Artefato: experiments/faixa2_vol_accelerator_test.json.
    delta: 1
  - id: 15
    date: "2026-08-09"
    type: selection_mechanism_variant
    description: >
      Faixa 2, pré-FASE 3 — correção do id 14: o Manager identificou que
      o teste anterior testou C07 como SUBSTITUTO do gate de confiança
      sobre TODAS as barras, e que o percentil 1 resultante caía numa
      região onde a geometria de barreira já não é válida (stop=1,5xATR
      abaixo do piso de quantização R1 — verificado: ATR% mediano 0,093
      no corte vs 0,405 na mediana geral, custo/ATR 62% vs 14%). Teste
      corrigido: COMPOR, não substituir — gate de confiança de produção
      INTACTO (`side_hat` real de `alpha_c1_v1`), C07 filtra SOMENTE o
      conjunto JÁ DISPARADO (~1,89% das barras). Percentil (85,3%)
      fixado a priori pelo mesmo orçamento (trades/ano), retendo 57,9%
      do conjunto disparado. Controle aleatório sorteia do MESMO
      conjunto disparado (não do universo), 1000 sementes.
      Resultado: CONFIRMA — pooled `directional_sharpe` +0,879 -> +3,015
      (percentil 100 vs controle aleatório), `ret_net` -2,82 -> -0,61 bps
      (percentil 99,7). Mas ASSIMÉTRICO por lado: long melhora muito
      (`total_sharpe` -1,30 -> +0,50, `ret_net` -4,97 -> +1,07,
      POSITIVO), short piora (`total_sharpe` -0,16 -> -1,21, `ret_net`
      -0,50 -> -2,82) — o filtro remove desproporcionalmente long×R2
      (pior célula, -22,86bps) preservando long×R3 (melhor célula,
      +5,46bps), mas também remove short×R2 (que era BOM, +7,95bps) e
      degrada short×R4. Nenhuma promoção a produção — achado é ranking
      para FASE 3/desenho futuro (filtro possivelmente side-specific),
      não decisão. delta=1 (variante de mecanismo, mesmo raciocínio do
      id 14).
      Artefato: experiments/faixa2_vol_accelerator_test_composed.json.
    delta: 1
  - id: 16
    date: "2026-08-17"
    type: canonical_estimator_reopened
    description: >
      `canonical_volatility_estimator` (M1, hoje `garman_klass_w20`,
      `constants.yaml` class A) reaberto sob grade dollar-bar (`AG-036`,
      remediação completa aprovada pelo Manager 2026-08-16). Walk-forward
      real, 5 símbolos × 3 resoluções (R1/R2/R3) × 6 candidatos
      (RealizedVol/ATRWilder/Parkinson/RogersSatchell/HAR-RV/EGARCH-
      acoplado) contra baseline GarmanKlass — `experiments/
      volatility_dollar_bar_report.json`, `AG-065`/`AG-074`. Contagem:
      candidatos (6) × resoluções (3) = 18 "pontos" que exigem forecast/
      fit novo contra dado real cada um (mesmo grão de `id 10`, grade
      tp_atr_mult×sl_atr_mult contada por PONTO DE PARÂMETRO, não por
      símbolo) — símbolos (5) são o leito de teste OOS/robustez, não
      contados multiplicativamente (mesmo raciocínio de "folds dentro de
      uma combinação não multiplicam a contagem", já implícito no ledger
      inteiro: nenhuma entrada aqui multiplica por nº de folds do
      walk-forward). Cobre TAMBÉM a 1ª medição parcial (4 candidatos,
      só R1, `AG-065` original, 2026-08-17 mais cedo na mesma sessão) —
      nunca tinha sido registrada no ledger (gap confirmado por
      `project_assurance` antes desta entrada); registrado uma vez só
      pelo escopo final completo, não em duas entradas parciais.
      Resultado: Parkinson bate GK (significativo, `dm_p<0,05`) em 12/15
      combinações; empate estatístico (bate na média mas `dm_p>0,05`) em
      2/15 (ETHUSDT×R3, XRPUSDT×R3); GK vence sem contestação em 1/15
      (SOLUSDT×R3). Nenhuma promoção de `canonical_volatility_estimator`
      feita aqui — decisão fica com o Manager (`AG-065`).
      Nota de contagem, não decisão do Manager ainda validada: se o
      Manager preferir contar por (símbolo×resolução×candidato) = 90, ou
      só por resolução (candidatos comparados = 1 passe de ranking por
      resolução, delta=3), ajustar numa entrada FUTURA (nunca editar
      esta) — a regra do ledger ainda deixa espaço de julgamento aqui,
      registrado explicitamente para não esconder a ambiguidade.
    delta: 18
  - id: 17
    date: "2026-08-17"
    type: budget_override_manager
    description: >
      `counter` (63, após a entrada id 16 acima) excede o orçamento total
      declarado da V4.1 (`PRD_V4_1.md:625`, `N_lifetime final = 60`) e
      dispara o critério de encerramento §6.5 item 5 ("`N_lifetime` > 60
      sem Camada 2 fechada → encerrar"; Camada 2/Meta não fechou). Manager
      autorizou explicitamente estourar o orçamento pra migração
      Parkinson+dollar-bar especificamente ("Autorizo estourar o
      orçamento, seguir 100%"), com a justificativa registrada nesta
      conversa: a migração aplica uma decisão JÁ MEDIDA (id 16 acima —
      Parkinson bate GK, `AG-065`/`AG-074`), não explora edge nova — não
      é o tipo de gasto de orçamento que o critério §6.5-5 existe pra
      conter (buscas exploratórias sem fim declarado). `delta=0`: esta
      entrada registra a AUTORIZAÇÃO em si, que não consome trial —
      retreino real subsequente (quando a Fase 5 da migração de fato
      rodar, ver `docs/refactor_parkinson_canonico.md`,
      `audit/architecture_gaps_log.yaml::AG-036::
      addendum_engenharia_pronta_producao_adiada_2026_08_17`) conta
      normalmente, NÃO é isento por este override. Engenharia da migração
      (Fases 0-4, Label/Feature/Regime Engine + orquestração) implementada
      e testada nesta mesma sessão (commits `5df33c3`/`3449471`/
      `9a4c3c5`/`b5760fe`); Fase 5 (flip de `value` + reprocessamento real
      + retreino) deliberadamente NÃO executada — Manager decidiu adiar
      pra rodada futura já agendada no roadmap, junto de outras mudanças
      previstas, em vez de gastar N_lifetime/tempo num run isolado agora.
    delta: 0

# Nota de escopo (Faixa 2, F0.1, 2026-08-09): os 4 itens acima (ids 6-9,
# delta total +18, contador 5->23) são exatamente os listados pela task
# "Faixa 2" como pendentes de auditoria — NÃO inclui as demais medições
# exploratórias da Faixa 1.7 (B1/B2 por regime, IC de E02f carry vs.
# direcional, R2 por ano, distribuição de feature R2 vs. tendência,
# NOFILL x tercil de custo, a extensão "gap Alpha-menos-passivo" de 8
# células + 4 splits de tendência por lado×regime). Essas ficam fora
# desta auditoria por instrução explícita do escopo desta task, não por
# terem sido esquecidas — mesma pergunta de escopo já registrada como
# aberta na "Revisão externa da Faixa 1.6" do SPRINT_LOG (linha ~1086),
# agora resolvida PARCIALMENTE (só os 4 itens nomeados), não por completo.
# Reabrir se o Manager decidir que o escopo deveria ser exaustivo.
#
# Nota de proveniência do seed: os 3 trials 1-3 reconstroem o histórico
# citado em §18.6 M5 e §18.7 item 9 do PRD ("já há trials gastos"). É uma
# reconstrução parcial, não um registro contemporâneo — o histórico completo
# anterior a este repo (V2 → V3.0 → V3.1 → V3.2) não é auditável linha a linha.
# Tratar `counter` como PISO, não como valor exato.
  - id: 18
    date: "2026-08-23"
    type: retreino_real
    description: >
      Primeiro retreino real do Alpha pos-migracao LightGBM (D-01 a D-18),
      gate "Data Layer 100%" fechado -- 15 combinacoes (5 simbolos x
      {R1,R2,R3}), cada uma um AJUSTE DE MODELO real (Camada1 + Camada0,
      LightGBM, CPCV de 5 paths) + BACKTEST novo por combinacao -- conta
      como N trials (regra de contagem deste ledger, nao 1 passe de
      ranking). Resultado: 3 de 15 (20%) passam o gate de permanencia
      (ETHUSDT/R1, SOLUSDT/R2, SOLUSDT/R3) -- detalhe completo em
      `audit/evidence_ledger.yaml::alpha-lightgbm-sweep-15-combinacoes-
      2026-08-23`. Contador nao e mais gate vinculante desde 2026-08-17
      (AG-077, status deste arquivo) mas continua incrementado -- DSR
      (`src/validation/dsr.py`) le `counter` de verdade, decisao do que
      substitui a penalidade de multiple-testing segue pendente do
      Manager (AG-077, nao resolvida por este incremento).
    delta: 15
  - id: 19
    date: "2026-08-24"
    type: sweep_sensibilidade_classe_a
    description: >
      S1 -- sweep de sensibilidade tp_atr_mult/sl_atr_mult (reparametrizado
      R=reward_risk_ratio x S=sl_mult, docs/s1_design_doc_sweep_tp_sl_
      reward_risk_ratio_2026-08-22.md), CLAUDE.md SS16.10 regra 4 (constante
      classe A com sweep_required=true, nunca varrida desde Sprint 6).
      Verificacao de robustez ao redor do ponto ja escolhido (celula central
      R=4/3,S=3/2 -> tp=2.0,sl=1.5 = producao, reproducao exata confirmada
      -- sanidade_centro_da_grade.reproduz_producao_exato=true), NUNCA busca
      de novo otimo. Grade declarada a priori, 9 celulas, 2 universalmente
      excluidas (fora do sweep_range de tp_atr_mult), 7 celulas x 5 simbolos
      x 2 lados -- BTCUSDT/BNBUSDT perdem mais 2 celulas cada (sl_mult=3/4
      viola o piso R2 pra esses 2 simbolos especificamente, medido com ATR
      fresco). Delta aplicado seguindo o CRITERIO MECANICO ja escrito no
      cabecalho deste arquivo (recalculo de backtest por combinacao) --
      resolve_barriers_vectorized chamado separadamente por (celula,lado):
      9 celulas x 2 lados = 18, mesmo criterio usado no precedente id=10.
      3 leituras concorrentes ficaram registradas no design doc (9/18/1,
      SS11 risco #8) sem reconciliacao pelo autor -- delta=18 aplicado aqui
      por ser a leitura que bate com o criterio ja escrito, nao por
      reconciliar a favor de uma das 3 por julgamento novo. Resultado:
      TODAS as 7 celulas tem edge_atr_units medio NEGATIVO entre os
      estratos symbol x lado -- inclusive a celula de producao
      (edge_mean=-0,0255). Achado adicional: long e sistematicamente pior
      que short na geometria de producao nas 5 moedas (0 de 5 longs
      positivos, 3 de 5 shorts positivos). Veredito de "sobrevive a faixa"
      fica TBD (SS11 risco #1, decisao do Manager, nao computada). Detalhe:
      audit/evidence_ledger.yaml::s1-tp-sl-sensitivity-2026-08-24.
    delta: 18
  - id: 20
    date: "2026-08-24"
    type: fase0_diagnostico_ruido
    description: >
      Fase 0 da ablacao T2->T1 (docs/t2_t1_ablation_veredito_duas_
      analises_2026-08-24.md SS4) -- diagnostico de ruido ANTES de
      qualquer sweep de k/hiperparametro, ETHUSDT/R1, k=7 (T1 atual)
      travado, hiperparametro PROD travado (nao e busca, e medicao de
      ruido de execucao). 0a -- repeticao de seed: 10 execucoes
      completas (15 splits x 2 lados = 30 fits cada), so `seed` varia.
      0b -- nulo por permutacao de rotulo: 50 execucoes, `label`+
      `ret_net` permutados juntos (mesmo indice), Camada1 E Camada0
      recebendo a MESMA permutacao por repeticao (mesmo criterio de
      contagem do id=18 -- par Camada1+Camada0 = 1 trial por
      combinacao, nao 2). delta = 10+50 = 60, cada execucao um
      retreino real completo (recalculo de backtest por combinacao,
      mesmo criterio mecanico do cabecalho deste arquivo).
      Resultado 0a: pooled_sharpe mean=-1,3658 std=0,3060 (n=10) --
      ruido de seed puro (k/hiperparametro fixos) ja produz desvio-
      padrao de ~0,31 em unidades de Sharpe; qualquer comparacao futura
      de configuracao precisa superar essa ordem de grandeza pra nao
      ser ruido. Resultado 0b: distribuicao empirica de n_better sob
      nulo verdadeiro (5 paths, correlacao real do CPCV embutida, sem
      assumir Binomial) = {0:1, 1:13, 2:21, 3:10, 4:4, 5:1} (n=50),
      media=2,12 (ABAIXO do 2,5 que Binomial(5;0,5) preveria --
      Camada1 com monotone_constraints tende a perder MAIS que 50% das
      vezes sob ruido puro, achado novo). P(n_better>=4 | nulo) empirico
      = 5/50 = 0,10 -- limiar atual (`alpha_layer1_permanence_min_paths`)
      tem falso-positivo medido de ~10% nesta combinacao especifica, nao
      calibrado antes. Achado adicional: o resultado REAL de producao
      ETHUSDT/R1 (2026-08-23) foi 5/5 -- só 1/50 (2%) das repeticoes do
      nulo bateram 5/5, mais extremo que o limiar >=4 sozinho sugeria --
      evidencia mais forte de sinal real nesta combinacao especifica do
      que a leitura agregada "3/15 e ruido" (SS2.4 do design doc) tinha
      capturado. NAO decide limiar de producao sozinho -- achado bruto
      pro AG-NNN, decisao do Manager (3 razoes registradas no design
      doc: limiar condicionado a k=7 caduca na Fase 1; resultado
      provavel e "criterio errado", nao "numero errado"; mudar gate por
      experimento proprio e decisao de graus de liberdade do projeto).
      Achado de infraestrutura, sem custo de trial: LightGBM deste
      ambiente sem CUDA compilado, rodado em `device_type=cpu` (~29s por
      execucao completa, medido -- nao os ~2min sinteticos citados antes
      da medicao real). Artefato: experiments/noise_floor_diagnostics_
      ETHUSDT_R1.json (reconstruido do log estruturado apos achado real
      de bug -- orjson exige chave dict str, backtest_by_path devolve
      dict[int,float] -- descoberto so na escrita final, compute das 60
      execucoes ja tinha terminado corretamente antes disso; corrigido
      em src/validation/noise_floor_diagnostics.py::_pooled_sharpe).
    delta: 60
  - id: 21
    date: "2026-08-24"
    type: feature_orthogonality_research_pass
    description: >
      Pré-requisito da Fase 1 da ablação T2->T1 (`docs/t2_t1_ablation_
      veredito_duas_analises_2026-08-24.md` SS4) -- ranking dos 62
      candidatos T2 (`SUPPORT_FEATURE_IDS`) por estabilidade
      (`src.models.stability.stability_screen`, IN-FOLD, 15 splits x 2
      lados REAIS do CPCV = 30 celulas por feature, mesmo precedente do
      E3/id 13) + filtro de ortogonalidade (|Spearman|<=0,70, novo
      `alpha_t2_orthogonality_spearman_max`, `config/constants.yaml`).
      Nenhum modelo retreinado, nenhuma promocao a T1_FEATURE_IDS/
      registry.yaml -- passe de ranking/triagem, mesmo criterio de
      contagem dos ids 11/13 ("medicao pra a triagem in-fold decidir",
      1 trial nao 62). ETHUSDT/R1. Resultado: 39 de 62 sobrevivem ao
      filtro de ortogonalidade; conjuntos k=6,9,12,16,24 construidos
      (top-k da lista ordenada por estabilidade, ja filtrada) -- usados
      como grade de features da Fase 1 (mapa de capacidade).
      Achado real corrigido no processo (nao remediado, causa raiz):
      schema por-arquivo inconsistente em `data/capacity/metrics/
      {symbol}/*.parquet` -- alguns arquivos diarios serializam colunas
      de futures-positioning como String em vez de Float64 (valores
      numericos legitimos, so dtype inconsistente entre arquivos,
      inferencia por-arquivo sem schema declarado). Sistemico nos 5
      simbolos (1-3 arquivos de ~1700-2200 cada por simbolo, confirmado
      por varredura), nao so ETHUSDT -- corrigido em
      `src/features/_sources.py::_load_and_dedupe_metrics_rows` (cast
      explicito por arquivo antes do concat, com warning estruturado se
      algum arquivo precisou de recast). Artefato: experiments/
      t2_ranking_ortogonalidade_ETHUSDT_R1.json.
    delta: 1
  - id: 22
    date: "2026-08-24"
    type: fase1_mapa_capacidade
    description: >
      Fase 1 da ablacao T2->T1 (`docs/t2_t1_ablation_veredito_duas_
      analises_2026-08-24.md` SS4) -- mapa de capacidade, ETHUSDT/R1, grid
      enumerado max_depth={2,3,4,5,6} x num_leaves condicionado (nunca
      >2^max_depth, nunca so no teto) x k={6,9,12,16,24} (conjuntos do
      ranking id=21) = 65 combinacoes, so Camada1 (mapa de FORMA da
      superficie de Sharpe, nao comparacao de permanencia) + 1 trial
      extra (min_child_samples=200 na melhor combinacao do grid).
      delta=66, cada combinacao um retreino real completo (15 splits do
      CPCV, recalculo de backtest -- mesmo criterio mecanico do
      cabecalho deste arquivo).
      Resultado -- padrao limpo e SEM excecao nas 65 combinacoes: (1) `k`
      maior sempre melhora Sharpe, monotonico em TODAS as 13 linhas do
      grid (nenhuma excecao); (2) mais complexidade de arvore (max_depth/
      num_leaves maiores) sempre piora, tambem monotonico e sem excecao
      -- confirma empiricamente o que as 2 analises criticas (usuario +
      auditoria externa) preveram ("aumentar max_depth pode so
      transformar 0,51 de AUC em overfitting mais sofisticado"). Melhor
      combinacao do grid inteiro: k=24, max_depth=2, num_leaves=2 (os
      MENORES valores testados de ambos), pooled_sharpe=-2,2073. Trial
      extra (min_child_samples=200 vs 20 PROD, mesma combinacao):
      pooled_sharpe=-1,9762, delta=+0,2311 -- mais regularizacao ajuda
      ainda mais, mesma direcao do padrao.
      Achado que pesa contra promocao: MESMO a melhor combinacao
      (-1,9762) fica pior que a media do piso de ruido medido na Fase 0a
      (k=7 T1 atual, hiperparametro PROD, pooled_sharpe mean=-1,3658
      std=0,3060) -- cerca de 2 desvios-padrao do ruido de seed puro
      medido, nao dentro do ruido. Nenhuma das 65+1 combinacoes testadas
      com features T2 (nunca usadas em producao) supera o T1 atual de 7
      features neste ativo/resolucao. Regra de parada do design doc
      (SS4, "sem regiao consistente ate o fim da Fase 1, nao escala pra
      Fase 2") tecnicamente NAO se aplica pela letra (existe relacao
      k/depth CONSISTENTE, so nao favoravel a promocao) -- decisao sobre
      escalar pra Fase 2 ou nao fica pro Manager, nao decidida aqui.
      Artefato: experiments/t2_t1_capacity_map_ETHUSDT_R1.json.
    delta: 66
